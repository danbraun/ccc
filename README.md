# ccc
Counterculture Collective website

## Dev Setup
Currently minimally setup. Manually deploy with:
```npx @11ty/eleventy```

Default ```_site/``` folder is used for build. Run site with VSCode's Live Server which is configured in ```.vscode/settings.json``` to use ```_site/``` as root.